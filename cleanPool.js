var trash = false;
var dirtyWater = false;
var cleanPool = !trash && !dirtyWater; // the pool is clean if there is no trash and no dirty water
console.log(cleanPool); // output the result to the console